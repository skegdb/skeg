name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: skegdb/.github/.github/workflows/rust-ci.yml@main
    with:
      workspace: true
      msrv: "1.88"
